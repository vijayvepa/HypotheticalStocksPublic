# Docker Setup

## Hosts File

- For now, update hosts file in `%WINDIR%/System32/drivers/etc/hosts`

```hosts
127.0.0.1 portfolio
127.0.0.1 keycloak

# optional for testing with docker profile
127.0.0.1 mongo
```